[build]
  publish = "dist"
  command = "pip install -r requirements.txt && flet build web"

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
