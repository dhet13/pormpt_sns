[build]
  publish = "dist"
  command = "pip install --upgrade pip && pip install -r requirements.txt && python build.py"

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
